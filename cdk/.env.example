# ============================================
# AWS CDK Environment Configuration
# ============================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ============================================
# AWS Configuration
# ============================================

# AWS SSO Profile (from ~/.aws/config)
# Run: aws configure sso
# Then: aws sso login --profile YOUR_PROFILE
AWS_PROFILE=your-aws-profile-name

# AWS Region to deploy infrastructure
# Common: us-east-1, us-west-2, eu-west-1
AWS_REGION=your-region

# ============================================
# Deployment Environment
# ============================================

# Environment name (affects resource naming)
# Options: prod, staging, dev
# - prod: Multi-AZ RDS, 2+ ECS tasks, deletion protection
# - staging: Same as prod but different stack
# - dev: Single-AZ RDS, 1 ECS task, no deletion protection
ENVIRONMENT=dev

# ============================================
# SSL/TLS Configuration (Optional)
# ============================================

# ACM Certificate ARN for HTTPS (optional)
# If not provided, ALB will only listen on HTTP (port 80)
# If provided, ALB will:
#   - Listen on HTTPS (port 443) with this certificate
#   - Redirect all HTTP traffic to HTTPS
#
# To create a certificate:
#   1. Go to AWS Certificate Manager in your deployment region
#   2. Request a public certificate for your domain (e.g., api.reporeconnoiter.com)
#   3. Validate via DNS (add CNAME records to your domain)
#   4. Copy the ARN here once issued
#
# Example: ACM_CERTIFICATE_ARN=arn:aws:acm:us-west-2:123456789012:certificate/abc-123-def-456
# ACM_CERTIFICATE_ARN=

# ============================================
# Notes
# ============================================
# - This file is gitignored for security
# - All variables except ACM_CERTIFICATE_ARN are required
# - CDK will fail if required variables are missing
