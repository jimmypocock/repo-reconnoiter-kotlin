# Environment Variables for RepoReconnoiter Kotlin API
# Copy this file to .env and fill in your actual values
# .env is gitignored and will not be committed

# Spring Profile (dev or prod)
SPRING_PROFILES_ACTIVE=dev

# Database Configuration
# Note: DATABASE_URL uses 'localhost' for local development, 'mysql' for Docker
DATABASE_URL=jdbc:mysql://localhost:3306/reconnoiter_dev
DATABASE_USERNAME=reconnoiter
DATABASE_PASSWORD=devpassword
MYSQL_DATABASE=reconnoiter_dev
MYSQL_ROOT_PASSWORD=rootpassword

# GitHub OAuth (get from https://github.com/settings/developers)
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# JWT Configuration
# Generate a secure random string (at least 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_at_least_32_characters_long_for_hs256

# Frontend Configuration
# Next.js frontend URL for CORS and OAuth redirects (no trailing slash)
# Development: http://localhost:3000
# Production: https://your-nextjs-app.com
APP_FRONTEND_URL=http://localhost:3000

# Sentry Error Tracking Configuration
# Two tokens are needed for complete Sentry integration:

# 1. SENTRY_AUTH_TOKEN - Used at BUILD TIME for Docker builds
#    - Uploads source code to Sentry for better stack traces
#    - Only needed for PRODUCTION Docker builds (not development)
#    - Get from: https://sentry.io/settings/account/api/auth-tokens/
#    - Required permissions: project:releases, project:write
#    - Leave EMPTY for development (skips source uploads)
SENTRY_AUTH_TOKEN=

# 2. SENTRY_DSN - Used at RUNTIME to send errors to Sentry
#    - Leave EMPTY in development (errors show in console only)
#    - Set in production to enable error tracking
#    - Get from: https://sentry.io/settings/[org]/projects/[project]/keys/
#    - Example: https://abc123def456@o123456.ingest.sentry.io/789012
SENTRY_DSN=
